---
export interface Props {
    options: { label: string; href: string }[];
    active: string;
    transitionName: string;
}
const { options, active, transitionName } = Astro.props;
---
<div class="inline-flex items-center border border-line rounded-xs overflow-hidden font-mono text-xs uppercase tracking-wider">
    {options.map(opt => {
        const isActive = opt.href === active;
        return isActive ? (
            <span
                transition:name={transitionName}
                class="px-4 py-2 bg-ink text-bg cursor-default"
                aria-current="page"
            >
                {opt.label}
            </span>
        ) : (
            <a
                href={opt.href}
                class="px-4 py-2 text-muted hover:text-ink hover:bg-accent-soft transition-colors duration-150"
            >
                {opt.label}
            </a>
        );
    })}
</div>
